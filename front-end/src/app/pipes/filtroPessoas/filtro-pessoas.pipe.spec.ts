import { FiltroPessoasPipe } from './filtro-pessoas.pipe';

describe('FiltroPessoasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPessoasPipe();
    expect(pipe).toBeTruthy();
  });
});
